[project]
name = "newfan-normalizers"
version = "0.1.0"
description = "正規化器レジストリ（§5.6）"
requires-python = ">=3.12"
dependencies = [
    "newfan-schemas",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/newfan_normalizers"]
